# Only needed when running the API or Prisma CLI outside Docker.
# Inside Docker, docker-compose.yml supplies DATABASE_URL and points at the `db` service.
# Must match the credentials in the root .env file.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/phoneme_builder
