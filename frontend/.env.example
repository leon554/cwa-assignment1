# Base URL the browser uses to reach the API. The API is published on port 80 by docker-compose.
NEXT_PUBLIC_API_URL=http://localhost:80

# Base URL used for server-side rendering, where the API is reachable by its compose service name.
# docker-compose.yml sets this automatically; only needed when running outside Docker.
INTERNAL_API_URL=http://localhost:80
